class Button : ZilchComponent
{
    function Initialize(init : CogInitializer)
    {
        Zero.Connect(this.Owner, "MyCoolEvent", this.OnMyCoolEvent);
    }

    function OnMyCoolEvent(event : MyCoolEvent)
    {
        Console.WriteLine(event.Damage);
    }
}
